Работа со слоем view.
Нам нужно знать:
 - как подписывать на LiveData, для получения данных из view-model
 - состояние обработки результата запроса. Эти состояния описаны в классе Result.

 Полностью реализовали слой view